interface:
  display_name: "git commit"
  short_description: "Inspect repos, prepare focused commits, and guide safe Git actions with direct Git commands"
  default_prompt: "Use the `git commit` skill to inspect this repository, prepare a focused commit, and handle safe merge decisions with direct Git commands only."
policy:
  allow_implicit_invocation: true
